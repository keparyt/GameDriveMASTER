const grid=document.querySelector("#grid"),search=document.querySelector("#search"),online=document.querySelector("#online"),errorBox=document.querySelector("#error"),drivesBox=document.querySelector("#drives"),driveCount=document.querySelector("#drive-count"),modal=document.querySelector("#game-modal"),detailHero=document.querySelector("#detail-hero"),detailLogo=document.querySelector("#detail-logo"),detailTitle=document.querySelector("#detail-title"),detailMeta=document.querySelector("#detail-meta"),detailDescription=document.querySelector("#detail-description"),detailTrailer=document.querySelector("#detail-trailer"),trailerSection=document.querySelector("#trailer-section"),launchButton=document.querySelector("#launch-game"),launchStatus=document.querySelector("#launch-status"),headerTime=document.querySelector("#header-time");let selectedGameId=null,closeTimer=null,previousDriveState=null,drivePollTimer=null,artworkPollTimer=null,currentGamesSignature=null,loadGamesRequest=0,artworkRequest=0;
function escapeHtml(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}function showError(m){errorBox.textContent=m;errorBox.style.display="block"}function hideError(){errorBox.textContent="";errorBox.style.display="none"}function formatBytes(bytes){let n=Number(bytes||0),u=["B","GB","TB","PB"],i=0;while(n>=1024&&i<u.length-1){n/=1024;i++}return n?`${n>=10||i===0?Math.round(n):n.toFixed(1)} ${u[i]}`:"—"}function clearCloseTimer(){if(closeTimer){clearInterval(closeTimer);closeTimer=null}}function driveState(ds){return ds.map(d=>`${d.uuid||d.id}:${Boolean(d.connected)}`).sort().join("|")}function gameQuery(){return new URLSearchParams({q:search.value.trim(),connected_only:online.checked})}function gameSignature(ds){return JSON.stringify(ds.map(g=>({id:g.id,title:g.title,name:g.name,connected:Boolean(g.connected),drive_name:g.drive_name,last_letter:g.last_letter,cover:g.cover,capsule:g.capsule,logo:g.logo}))) }
function showDriveNotification(name,connected){let toast=document.querySelector("#drive-notification");toast.className=`drive-notification ${connected?"is-connected":"is-disconnected"}`;toast.innerHTML=`<span class="drive-notification-icon">${connected?"✓":"!"}</span><span><strong>${escapeHtml(name||"GameDrive")}</strong><small>${connected?"Drive connected":"Drive disconnected"}</small></span>`;requestAnimationFrame(()=>toast.classList.add("show"));clearTimeout(toast._hideTimer);toast._hideTimer=setTimeout(()=>toast.classList.remove("show"),4500)}
async function checkDriveChanges(){try{const r=await fetch("/api/drives",{cache:"no-store"});if(!r.ok)return;const ds=await r.json(),state=driveState(ds);if(previousDriveState!==null&&state!==previousDriveState){const old=new Map(previousDriveState.split("|").filter(Boolean).map(x=>{const p=x.split(":");return[p[0],p[1]==="true"]}));for(const d of ds){const k=String(d.uuid||d.id),before=old.get(k),now=Boolean(d.connected);if(before!==undefined&&before!==now)showDriveNotification(d.name,now)}previousDriveState=state;await loadDrives(false);await loadGames({force:true})}else previousDriveState=state}catch(e){console.debug("Drive check failed",e)}}
function createDriveCard(d){const c=Boolean(d.connected),letter=d.last_letter?`${d.last_letter}:`:"—",description=d.description||"No description";return `<div class="drive-chip ${c?"connected":"disconnected"}" title="${escapeHtml(description)}"><span class="drive-dot"></span><div class="drive-chip-info"><strong>${escapeHtml(d.name||"Unnamed GameDrive")}</strong><small>${escapeHtml(letter)} · ${c?"Connected":"Offline"}</small><small>${escapeHtml(description)}</small></div></div>`}function createHardwareCard(d){const c=!d.offline&&String(d.status).toLowerCase().includes("online");return `<div class="hardware-chip ${c?"connected":"disconnected"}"><span class="hardware-icon">▣</span><div class="drive-chip-info"><strong>${escapeHtml(d.name||"Unknown disk")}</strong><small>${escapeHtml(d.bus||"Unknown")} · ${escapeHtml(formatBytes(d.size))} · ${escapeHtml(d.health||"Unknown")}</small></div></div>`}
async function loadDrives(update=true){const[a,b]=await Promise.all([fetch("/api/drives",{cache:"no-store"}),fetch("/api/system/disks",{cache:"no-store"})]);if(!a.ok||!b.ok)throw Error("Unable to load drive information");const ds=await a.json(),hw=await b.json(),connected=ds.filter(d=>Boolean(d.connected)).length;driveCount.textContent=`${connected} connected / ${ds.length} partitions`;drivesBox.innerHTML=`<div class="drive-group"><div class="drive-group-title">GameDrive partitions</div><div class="drive-list">${ds.map(createDriveCard).join("")||'<span class="muted">No GameDrive partitions indexed yet.</span>'}</div></div><div class="drive-group"><div class="drive-group-title">Physical hardware</div><div class="drive-list">${hw.map(createHardwareCard).join("")||'<span class="muted">No physical disks detected.</span>'}</div></div>`;if(update)previousDriveState=driveState(ds)}
function createCard(g,i){const c=Boolean(g.connected),cover=g.cover||g.capsule,logo=g.logo,title=g.title||g.name||"Unknown Game",drive=g.drive_name||"Unknown drive",letter=c&&g.last_letter?`${g.last_letter}:`:"Offline";return `<article class="card ${c?"":"offline-card"}" style="animation-delay:${Math.min(i*18,500)}ms" data-game-id="${g.id}" tabindex="0" role="button" aria-label="Open ${escapeHtml(title)}"><div class="art">${cover?`<img src="${escapeHtml(cover)}" alt="${escapeHtml(title)}" loading="lazy">`:'<div class="fallback artwork-loading">LOADING ARTWORK…</div>'}</div><div class="info">${logo?`<img class="logo" src="${escapeHtml(logo)}" alt="" loading="lazy">`:""}<div class="title" title="${escapeHtml(title)}">${escapeHtml(title)}</div><div class="drive">${escapeHtml(drive)} · ${escapeHtml(letter)}</div><div class="badge ${c?"":"offline"}">${c?"CONNECTED":"OFFLINE"}</div></div></article>`}
function patchGames(ds){const cards=[...grid.querySelectorAll(".card")],ids=cards.map(c=>String(c.dataset.gameId));const newIds=ds.map(g=>String(g.id));if(ids.length!==newIds.length||ids.some((id,i)=>id!==newIds[i])){grid.innerHTML=ds.length?ds.map(createCard).join(""):`<div class="empty">No games found.</div>`;return true}ds.forEach(g=>{const card=grid.querySelector(`.card[data-game-id="${CSS.escape(String(g.id))}"]`);if(!card)return;const c=Boolean(g.connected),badge=card.querySelector(".badge"),drive=card.querySelector(".drive");if(badge){badge.textContent=c?"CONNECTED":"OFFLINE";badge.className=`badge ${c?"":"offline"}`}if(drive)drive.textContent=`${g.drive_name||"Unknown drive"} · ${c&&g.last_letter?`${g.last_letter}:`:"Offline"}`});return false}
async function loadGames({force=false}={}){const requestId=++loadGamesRequest;const r=await fetch(`/api/games?${gameQuery()}`,{cache:"no-store"});if(requestId!==loadGamesRequest)return;if(!r.ok)throw Error(`HTTP ${r.status}`);const ds=await r.json(),signature=gameSignature(ds);if(!force&&signature===currentGamesSignature)return;currentGamesSignature=signature;patchGames(ds)}
async function refreshArtwork(){const requestId=++artworkRequest;try{const r=await fetch(`/api/games?${gameQuery()}`,{cache:"no-store"});if(requestId!==artworkRequest||!r.ok)return;const games=await r.json();for(const g of games){const card=grid.querySelector(`.card[data-game-id="${CSS.escape(String(g.id))}"]`);if(!card)continue;const art=card.querySelector(".art"),cover=g.cover||g.capsule;if(cover){const existing=art.querySelector("img");if(!existing||existing.getAttribute("src")!==cover){const img=document.createElement("img");img.src=cover;img.alt=g.title||g.name||"Game artwork";img.loading="lazy";img.onload=()=>{if(grid.querySelector(`.card[data-game-id="${CSS.escape(String(g.id))}"]`)===card)art.replaceChildren(img)}}}const logo=card.querySelector(".logo");if(g.logo&&(!logo||logo.getAttribute("src")!==g.logo)){const info=card.querySelector(".info"),newLogo=document.createElement("img");newLogo.className="logo";newLogo.src=g.logo;newLogo.alt="";newLogo.loading="lazy";info.prepend(newLogo)}}if(selectedGameId){const g=games.find(x=>Number(x.id)===Number(selectedGameId));if(g){const hero=g.hero||g.capsule||g.cover;if(hero&&detailHero.style.backgroundImage==="none")detailHero.style.backgroundImage=`url("${String(hero).replace(/"/g,'\\"')}")`;if(g.logo&&detailLogo.style.display==="none"){detailLogo.src=g.logo;detailLogo.style.display="block"}}}}catch(e){console.debug("Artwork refresh failed",e)}}
function startConnectedCountdown(){clearCloseTimer();let n=5;launchButton.disabled=true;launchStatus.className="launch-status countdown-status";launchStatus.textContent=`Connected · closing in ${n}s`;closeTimer=setInterval(()=>{n--;if(n<=0){clearCloseTimer();closeGame();return}launchStatus.textContent=`Connected · closing in ${n}s`},1000)}
async function openGame(id){clearCloseTimer();selectedGameId=id;modal.classList.add("open");modal.setAttribute("aria-hidden","false");document.body.classList.add("modal-open");detailTitle.textContent="Loading…";detailLogo.style.display="none";detailHero.style.backgroundImage="none";detailDescription.textContent="Loading Steam details…";detailMeta.textContent="";trailerSection.style.display="none";detailTrailer.pause();detailTrailer.removeAttribute("src");launchButton.disabled=true;launchStatus.textContent="";try{const r=await fetch(`/api/games/${id}/details`,{cache:"no-store"});if(!r.ok)throw Error("Unable to load game details");const g=await r.json();detailTitle.textContent=g.title||g.name||"Unknown Game";if(g.logo){detailLogo.src=g.logo;detailLogo.style.display="block"}const hero=g.hero||g.capsule||g.cover;if(hero)detailHero.style.backgroundImage=`url("${String(hero).replace(/"/g,'\\"')}")`;detailMeta.textContent=[g.release_date,g.drive_name,g.last_letter?`${g.last_letter}:`:null].filter(Boolean).join(" · ");detailDescription.innerHTML=g.description||"No description available.";if(g.trailer){detailTrailer.src=g.trailer;trailerSection.style.display="block"}if(g.connected)startConnectedCountdown();else{launchButton.disabled=true;launchStatus.className="launch-status";launchStatus.textContent="Drive offline"}}catch(e){console.error(e);detailTitle.textContent="Unable to load game";detailDescription.textContent=e.message;launchButton.disabled=true}}
function closeGame(){clearCloseTimer();modal.classList.remove("open");modal.setAttribute("aria-hidden","true");document.body.classList.remove("modal-open");detailTrailer.pause();detailTrailer.removeAttribute("src");selectedGameId=null}
grid.addEventListener("click",e=>{const card=e.target.closest(".card");if(card)openGame(Number(card.dataset.gameId))});grid.addEventListener("keydown",e=>{if((e.key==="Enter"||e.key===" ")&&e.target.closest(".card")){e.preventDefault();openGame(Number(e.target.closest(".card").dataset.gameId))}});document.querySelectorAll("[data-close-game]").forEach(x=>x.addEventListener("click",closeGame));document.addEventListener("keydown",e=>{if(e.key==="Escape"&&modal.classList.contains("open"))closeGame();if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==="k"){e.preventDefault();search.focus();search.select()}});launchButton.addEventListener("click",async()=>{if(!selectedGameId)return;clearCloseTimer();launchButton.disabled=true;launchStatus.textContent="Starting…";try{const r=await fetch(`/api/games/${selectedGameId}/launch`,{method:"POST"}),d=await r.json();if(!r.ok||!d.ok)throw Error(d.error||`HTTP ${r.status}`);launchStatus.textContent="Game started"}catch(e){launchStatus.textContent=`Unable to start: ${e.message}`}finally{setTimeout(()=>{if(selectedGameId)launchButton.disabled=false},500)}});
setInterval(()=>{headerTime.textContent=new Intl.DateTimeFormat(undefined,{hour:"2-digit",minute:"2-digit"}).format(new Date())},1000);let timer=null;search.addEventListener("input",()=>{clearTimeout(timer);timer=setTimeout(()=>{currentGamesSignature=null;loadGames({force:true})},150)});online.addEventListener("change",()=>{currentGamesSignature=null;loadGames({force:true})});async function load(){hideError();try{await loadDrives();await loadGames({force:true});clearInterval(drivePollTimer);drivePollTimer=setInterval(checkDriveChanges,5000);clearInterval(artworkPollTimer);artworkPollTimer=setInterval(refreshArtwork,3000)}catch(e){console.error(e);showError("Unable to load the complete Game Library.")}}load();