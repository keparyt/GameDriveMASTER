const grid=document.querySelector("#grid"),search=document.querySelector("#search"),online=document.querySelector("#online"),errorBox=document.querySelector("#error"),drivesBox=document.querySelector("#drives"),driveCount=document.querySelector("#drive-count"),modal=document.querySelector("#game-modal"),detailHero=document.querySelector("#detail-hero"),detailLogo=document.querySelector("#detail-logo"),detailTitle=document.querySelector("#detail-title"),detailMeta=document.querySelector("#detail-meta"),detailDescription=document.querySelector("#detail-description"),detailTrailer=document.querySelector("#detail-trailer"),trailerSection=document.querySelector("#trailer-section"),launchButton=document.querySelector("#launch-game"),launchStatus=document.querySelector("#launch-status"),headerTime=document.querySelector("#header-time");let selectedGameId=null,closeTimer=null,previousDriveState=null,drivePollTimer=null,artworkPollTimer=null,currentGamesSignature=null,currentDrivesSignature=null,gamesRequest=0,drivesRequest=0,artworkRequest=0,gamesBusy=false,drivesBusy=false;
const escapeHtml=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));
const showError=m=>{errorBox.textContent=m;errorBox.style.display="block"},hideError=()=>{errorBox.textContent="";errorBox.style.display="none"};
function formatBytes(bytes){let n=Number(bytes||0),u=["B","GB","TB","PB"],i=0;while(n>=1024&&i<u.length-1){n/=1024;i++}return n?`${n>=10||i===0?Math.round(n):n.toFixed(1)} ${u[i]}`:"—"}
function clearCloseTimer(){if(closeTimer){clearInterval(closeTimer);closeTimer=null}}
function driveState(ds){return ds.map(d=>`${d.uuid||d.id}:${Boolean(d.connected)}`).sort().join("|")}
function gameQuery(){return new URLSearchParams({q:search.value.trim(),connected_only:online.checked})}
function gameSignature(ds){return JSON.stringify(ds.map(g=>({id:g.id,title:g.title,name:g.name,connected:Boolean(g.connected),drive_name:g.drive_name,last_letter:g.last_letter,cover:g.cover,capsule:g.capsule,logo:g.logo})))}
function driveSignature(ds,hw){return JSON.stringify({ds:ds.map(d=>({id:d.uuid||d.id,name:d.name,connected:Boolean(d.connected),last_letter:d.last_letter,description:d.description})),hw:hw.map(d=>({id:d.id||d.name,name:d.name,status:d.status,offline:d.offline,bus:d.bus,size:d.size,health:d.health}))})}
function showDriveNotification(name,connected){const toast=document.querySelector("#drive-notification");toast.className=`drive-notification ${connected?"is-connected":"is-disconnected"}`;toast.innerHTML=`<span class="drive-notification-icon">${connected?"✓":"!"}</span><span><strong>${escapeHtml(name||"GameDrive")}</strong><small>${connected?"Drive connected":"Drive disconnected"}</small></span>`;requestAnimationFrame(()=>toast.classList.add("show"));clearTimeout(toast._hideTimer);toast._hideTimer=setTimeout(()=>toast.classList.remove("show"),4500)}
function createDriveCard(d){const c=Boolean(d.connected),letter=d.last_letter?`${d.last_letter}:`:"—";return `<div class="drive-chip ${c?"connected":"disconnected"}" title="${escapeHtml(d.description||"No description")}"><span class="drive-dot"></span><div class="drive-chip-info"><strong>${escapeHtml(d.name||"Unnamed GameDrive")}</strong><small>${escapeHtml(letter)} · ${c?"Connected":"Offline"}</small><small>${escapeHtml(d.description||"No description")}</small></div></div>`}
function createHardwareCard(d){const c=!d.offline&&String(d.status).toLowerCase().includes("online");return `<div class="hardware-chip ${c?"connected":"disconnected"}"><span class="hardware-icon">▣</span><div class="drive-chip-info"><strong>${escapeHtml(d.name||"Unknown disk")}</strong><small>${escapeHtml(d.bus||"Unknown")} · ${escapeHtml(formatBytes(d.size))} · ${escapeHtml(d.health||"Unknown")}</small></div></div>`}
function patchDrives(ds,hw){const sig=driveSignature(ds,hw);if(sig===currentDrivesSignature)return false;currentDrivesSignature=sig;driveCount.textContent=`${ds.filter(d=>Boolean(d.connected)).length} connected / ${ds.length} partitions`;const existing=[...drivesBox.querySelectorAll(".drive-chip")],wanted=ds.map(d=>String(d.uuid||d.id));if(existing.length!==wanted.length||existing.some((x,i)=>x.dataset.driveId!==wanted[i])){const wrap=document.createElement("div");wrap.innerHTML=`<div class="drive-group"><div class="drive-group-title">GameDrive partitions</div><div class="drive-list">${ds.map(d=>createDriveCard({...d,uuid:d.uuid||d.id})).join("")||'<span class="muted">No GameDrive partitions indexed yet.</span>'}</div></div><div class="drive-group"><div class="drive-group-title">Physical hardware</div><div class="drive-list">${hw.map(createHardwareCard).join("")||'<span class="muted">No physical disks detected.</span>'}</div></div>`;const chips=wrap.querySelectorAll(".drive-chip");chips.forEach((c,i)=>c.dataset.driveId=wanted[i]);drivesBox.replaceChildren(...wrap.children);return true}return false}
async function loadDrives({initial=false}={}){if(drivesBusy)return;drivesBusy=true;const request=++drivesRequest;try{const[a,b]=await Promise.all([fetch("/api/drives",{cache:"no-store"}),fetch("/api/system/disks",{cache:"no-store"})]);if(request!==drivesRequest||!a.ok||!b.ok)return;const ds=await a.json(),hw=await b.json(),state=driveState(ds);if(previousDriveState!==null&&state!==previousDriveState){const old=new Map(previousDriveState.split("|").filter(Boolean).map(x=>{const p=x.split(":");return[p[0],p[1]==="true"]}));ds.forEach(d=>{const k=String(d.uuid||d.id),before=old.get(k),now=Boolean(d.connected);if(before!==undefined&&before!==now)showDriveNotification(d.name,now)});previousDriveState=state;patchDrives(ds,hw);await loadGames()}else{previousDriveState=state;patchDrives(ds,hw)}}catch(e){if(initial)throw e;console.debug("Drive refresh failed",e)}finally{drivesBusy=false}}
function createCard(g,i){const c=Boolean(g.connected),cover=g.cover||g.capsule,logo=g.logo,title=g.title||g.name||"Unknown Game",drive=g.drive_name||"Unknown drive",letter=c&&g.last_letter?`${g.last_letter}:`:"Offline";return `<article class="card ${c?"":"offline-card"}" style="animation-delay:${Math.min(i*18,500)}ms" data-game-id="${escapeHtml(g.id)}" tabindex="0" role="button" aria-label="Open ${escapeHtml(title)}"><div class="art">${cover?`<img src="${escapeHtml(cover)}" alt="${escapeHtml(title)}" loading="lazy">`:'<div class="fallback artwork-loading">LOADING ARTWORK…</div>'}</div><div class="info">${logo?`<img class="logo" src="${escapeHtml(logo)}" alt="" loading="lazy">`:""}<div class="title" title="${escapeHtml(title)}">${escapeHtml(title)}</div><div class="drive">${escapeHtml(drive)} · ${escapeHtml(letter)}</div><div class="badge ${c?"":"offline"}">${c?"CONNECTED":"OFFLINE"}</div></div></article>`}
function patchGames(ds){const sig=gameSignature(ds);if(sig===currentGamesSignature)return false;currentGamesSignature=sig;const old=[...grid.querySelectorAll(".card")],ids=old.map(c=>String(c.dataset.gameId)),wanted=ds.map(g=>String(g.id));if(ids.length!==wanted.length||ids.some((id,i)=>id!==wanted[i])){const fragment=document.createDocumentFragment();if(ds.length){const holder=document.createElement("div");holder.innerHTML=ds.map(createCard).join("");while(holder.firstElementChild)fragment.appendChild(holder.firstElementChild)}else{const empty=document.createElement("div");empty.className="empty";empty.textContent="No games found.";fragment.appendChild(empty)}grid.replaceChildren(fragment);return true}ds.forEach(g=>{const card=grid.querySelector(`.card[data-game-id="${CSS.escape(String(g.id))}"]`);if(!card)return;const c=Boolean(g.connected),badge=card.querySelector(".badge"),drive=card.querySelector(".drive");card.classList.toggle("offline-card",!c);if(badge){badge.textContent=c?"CONNECTED":"OFFLINE";badge.className=`badge ${c?"":"offline"}`}if(drive)drive.textContent=`${g.drive_name||"Unknown drive"} · ${c&&g.last_letter?`${g.last_letter}:`:"Offline"}`});return true}
async function loadGames({initial=false}={}){if(gamesBusy)return;gamesBusy=true;const request=++gamesRequest;try{const r=await fetch(`/api/games?${gameQuery()}`,{cache:"no-store"});if(request!==gamesRequest||!r.ok)return;patchGames(await r.json())}catch(e){if(initial)throw e;console.debug("Game refresh failed",e)}finally{gamesBusy=false}}
async function refreshArtwork(){const request=++artworkRequest;try{const r=await fetch(`/api/games?${gameQuery()}`,{cache:"no-store"});if(request!==artworkRequest||!r.ok)return;const games=await r.json();for(const g of games){const card=grid.querySelector(`.card[data-game-id="${CSS.escape(String(g.id))}"]`);if(!card)continue;const art=card.querySelector(".art"),cover=g.cover||g.capsule;if(cover){const existing=art.querySelector("img");if(!existing||existing.getAttribute("src")!==cover){const img=new Image();img.src=cover;img.alt=g.title||g.name||"Game artwork";img.loading="lazy";img.onload=()=>{if(grid.querySelector(`.card[data-game-id="${CSS.escape(String(g.id))}"]`)===card)art.replaceChildren(img)}}}const logo=card.querySelector(".logo");if(g.logo&&(!logo||logo.getAttribute("src")!==g.logo)){const info=card.querySelector(".info"),newLogo=new Image();newLogo.className="logo";newLogo.src=g.logo;newLogo.alt="";newLogo.loading="lazy";newLogo.onload=()=>{if(grid.querySelector(`.card[data-game-id="${CSS.escape(String(g.id))}"]`)===card)info.prepend(newLogo)}}}if(selectedGameId){const g=games.find(x=>Number(x.id)===Number(selectedGameId));if(g){const hero=g.hero||g.capsule||g.cover;if(hero&&detailHero.style.backgroundImage==="none")detailHero.style.backgroundImage=`url("${String(hero).replace(/"/g,'\\"')}")`;if(g.logo&&detailLogo.style.display==="none"){detailLogo.src=g.logo;detailLogo.style.display="block"}}}}catch(e){console.debug("Artwork refresh failed",e)}}
async function openGame(id){clearCloseTimer();selectedGameId=id;modal.classList.add("open");modal.setAttribute("aria-hidden","false");document.body.classList.add("modal-open");detailTitle.textContent="Loading…";detailLogo.style.display="none";detailHero.style.backgroundImage="none";detailDescription.textContent="Loading Steam details…";detailMeta.textContent="";trailerSection.style.display="none";detailTrailer.pause();detailTrailer.removeAttribute("src");launchButton.disabled=true;launchStatus.textContent="";try{const r=await fetch(`/api/games/${id}/details`,{cache:"no-store"});if(!r.ok)throw Error("Unable to load game details");const g=await r.json();detailTitle.textContent=g.title||g.name||"Unknown Game";if(g.logo){detailLogo.src=g.logo;detailLogo.style.display="block"}const hero=g.hero||g.capsule||g.cover;if(hero)detailHero.style.backgroundImage=`url("${String(hero).replace(/"/g,'\\"')}")`;detailMeta.textContent=[g.release_date,g.drive_name,g.last_letter?`${g.last_letter}:`:null].filter(Boolean).join(" · ");detailDescription.innerHTML=g.description||"No description available.";if(g.trailer){detailTrailer.src=g.trailer;trailerSection.style.display="block"}if(g.connected){launchButton.disabled=false;launchStatus.className="launch-status";launchStatus.textContent="Connected · Ready to start"}else{launchButton.disabled=true;launchStatus.className="launch-status";launchStatus.textContent="Drive offline"}}catch(e){console.error(e);detailTitle.textContent="Unable to load game";detailDescription.textContent=e.message;launchButton.disabled=true}}
function closeGame(){clearCloseTimer();modal.classList.remove("open");modal.setAttribute("aria-hidden","true");document.body.classList.remove("modal-open");detailTrailer.pause();detailTrailer.removeAttribute("src");selectedGameId=null}
grid.addEventListener("click",e=>{const card=e.target.closest(".card");if(card)openGame(Number(card.dataset.gameId))});grid.addEventListener("keydown",e=>{if((e.key==="Enter"||e.key===" ")&&e.target.closest(".card")){e.preventDefault();openGame(Number(e.target.closest(".card").dataset.gameId))}});document.querySelectorAll("[data-close-game]").forEach(x=>x.addEventListener("click",closeGame));document.addEventListener("keydown",e=>{if(e.key==="Escape"&&modal.classList.contains("open"))closeGame();if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==="k"){e.preventDefault();search.focus();search.select()}});launchButton.addEventListener("click",async()=>{if(!selectedGameId)return;clearCloseTimer();launchButton.disabled=true;launchStatus.textContent="Starting…";try{const r=await fetch(`/api/games/${selectedGameId}/launch`,{method:"POST"}),d=await r.json();if(!r.ok||!d.ok)throw Error(d.error||`HTTP ${r.status}`);launchStatus.textContent="Game started"}catch(e){launchStatus.textContent=`Unable to start: ${e.message}`}finally{setTimeout(()=>{if(selectedGameId)launchButton.disabled=false},500)}});
setInterval(()=>{headerTime.textContent=new Intl.DateTimeFormat(undefined,{hour:"2-digit",minute:"2-digit"}).format(new Date())},1000);let searchTimer=null;search.addEventListener("input",()=>{clearTimeout(searchTimer);searchTimer=setTimeout(()=>{currentGamesSignature=null;loadGames()},180)});online.addEventListener("change",()=>{currentGamesSignature=null;loadGames()});async function load(){hideError();try{await Promise.all([loadDrives({initial:true}),loadGames({initial:true})]);clearInterval(drivePollTimer);drivePollTimer=setInterval(()=>loadDrives(),5000);clearInterval(artworkPollTimer);artworkPollTimer=setInterval(refreshArtwork,3000)}catch(e){console.error(e);showError("Unable to load the complete Game Library.")}}load();