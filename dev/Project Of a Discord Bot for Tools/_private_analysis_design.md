Design note: Discord does not support ephemeral responses to ordinary Message objects. The automatic game detector is triggered by a normal channel message, not an interaction. The private workflow therefore sends the analysis to the requester by DM before deleting the original channel input. Interaction follow-ups (selection confirmations) remain ephemeral where Discord supports them. The selection panel is kept for 24 hours or deleted immediately after every verified game has been resolved.